{% extends 'base.html.twig' %}

{% block body %}
    {% for auteur in auteurs %}
        <main class="my-5">
            <div class="container">
            
                <div class="row">
                    
                    <div class="col-md-9 mb-4">
                    
                        <section>
                            <!-- Post -->
                            <div class="row">
                                <div class="col-md-8 mb-4">
                                    <h5>{{ auteur.name }}</h5>
                                    <a href="{{ path('auteur_show',{'id':auteur.id}) }}" class="btn btn-primary">Lire la suit</a>
                                    <a href="{{ path('auteur_edit',{'id':auteur.id}) }}" class="btn btn-primary">Edit</a>
                                </div>
                            </div>
                        </section>  
                    </div>
                </div>
            </div>
        </main>
    {% endfor %} 
{% endblock %}
