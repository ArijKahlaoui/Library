{% extends 'base.html.twig' %}

{% form_theme formArticle 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <h1>Creation d'un Book</h1>

    {{ form_start(formArticle) }}

        {{form_row(formArticle.title, {'attr':{
            'placeholder': "Titre de l'article"}} ) }}
        
        {{form_row(formArticle.auteur, {'attr':{
                'placeholder': "l'auteur du Livre"}} ) }}
        
        {{form_row(formArticle.content, {'attr':{
                'placeholder': "Extrait du Livre"}} ) }}

        {{form_row(formArticle.image, {'attr':{
                'placeholder': "URL de l'image"}} ) }}

        <br/><button type="submit" class="btn btn-success">Ajouter Livre</button>

    {{ form_end(formArticle) }}

{% endblock %}